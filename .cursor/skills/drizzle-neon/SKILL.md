---
name: drizzle-neon
description: Drizzle + Postgres patterns when database is enabled. Inert until DB is added.
---

# Drizzle + Neon

**Status:** Database not included in starter.

When wired:

- Schema: `src/db/schema.ts`
- Migrations: `drizzle/` (generated — do not edit by hand)
- Client: `src/db/index.ts`

Workflow: edit schema → generate → migrate.
