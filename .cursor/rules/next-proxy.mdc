---
description: Use src/proxy.ts — not middleware.ts.
alwaysApply: true
---

# Next.js proxy

- Use **`src/proxy.ts`** with **`export function proxy`** and optional `export const config`.
- **Do not** add **`middleware.ts`**.
- Say **proxy** / **edge proxy**, not middleware.
