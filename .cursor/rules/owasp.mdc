---
description: OWASP-aware coding for all agents (Top 10 2025 + API + LLM)
alwaysApply: true
---

Before shipping routes, auth, DB, CSP, AI, or deps: load `.cursor/skills/owasp-security/SKILL.md` then `.cursor/skills/owasp-secure-coding/SKILL.md`.

Priority: access control → validation → secrets → CSP/headers → supply chain → safe errors/logs.

Never: client-only auth, raw SQL + user input, secrets in `NEXT_PUBLIC_*`, stack traces to clients.
