# blender-director

Cursor entry: **`/ops`** from the **user** stack (`~/.cursor/agents/orcha.md`). Task type stays `orcha`.

This repo keeps **product** `.cursor` only: `mcp.json`, `rules/blender-3d.mdc`, Blender/3D skills. No starter Orcha dump. Do **not** `pnpm sync-agents`.

OpenClaw **Orcha** is Telegram. **Forge** owns Flux/Blender gen.

Other machine: install the starter into that user’s `~/.cursor`. Do not copy Spark `~/.cursor`.
